# Core Data Validation and Parsing
pydantic>=2.0.0

# Testing Framework
pytest>=7.0.0

# Storage & Retrieval (Qdrant + FastEmbed + Flask)
flask>=3.0.0
qdrant-client>=1.18.0
fastembed>=0.8.0
python-dotenv>=1.0.0

# Reranker AI Pipeline (Optional if running in simulation/mock mode)
torch>=2.0.0
FlagEmbedding>=1.2.0
