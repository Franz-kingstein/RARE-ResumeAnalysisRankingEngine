# Core dependencies
fastembed>=0.4.0
numpy>=1.26.0
orjson>=3.10.0
pydantic>=2.7.0
pyyaml>=6.0
tqdm>=4.66.0

# GPU support (optional)
# onnxruntime-gpu>=1.17.0

# PDF extraction (optional) — install with: pip install "resume-embedding[pdf]"
# pymupdf>=1.24.0

# Image OCR (optional) — install with: pip install "resume-embedding[ocr]"
# Also requires the Tesseract binary: https://github.com/UB-Mannheim/tesseract/wiki
# pytesseract>=0.3.10
# Pillow>=10.0.0

# All optional format extras — install with: pip install "resume-embedding[all-formats]"

# Development dependencies
pytest>=8.0.0
ruff>=0.5.0
